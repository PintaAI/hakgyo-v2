import { RoutePlaceholder } from "~/components/routing/route-placeholder";

export default function SignInPage() {
  return (
    <RoutePlaceholder
      title="Sign in"
      description="Authentication UI will live here."
    />
  );
}
